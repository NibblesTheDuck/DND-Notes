# Config — contains API keys, personal vault paths — NEVER commit this
config.json

# PyInstaller build artifacts
dist/
build/
*.spec

# Python cache
__pycache__/
*.pyc
*.pyo

# Old EXE backups folder
Claude Igonre this/

# Built executables (build artifacts — rebuild from source)
DnD Notes.exe

# Installer is distributed via GitHub Releases, not committed to repo
DnDNotesSetup.exe

# Zip assets (large, not needed in repo)
*.zip

# Claude Code local settings
.claude/

*.svg
favicon_logoai/
favicon_logoai.zip
